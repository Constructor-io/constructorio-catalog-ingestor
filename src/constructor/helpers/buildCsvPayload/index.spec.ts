import { buildCsvPayload } from ".";

describe(buildCsvPayload, () => {
  it.todo("should return a CsvPayload");

  describe("when there are no groups", () => {
    it.todo("returns undefined for groups");
  });

  describe("when there are no items", () => {
    it.todo("returns undefined for items");
  });

  describe("when there are no variations", () => {
    it.todo("returns undefined for variations");
  });
});
